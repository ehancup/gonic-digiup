package server

const (
	rootPath = "/v1"

	RootAccount   = rootPath + "/accounts"
	RootPerson    = rootPath + "/persons"
	RootPublisher = rootPath + "/publishers"
	RootAuthors = rootPath + "/authors"
	RootForm = rootPath + "/form"

	PathLogin = "/login"
)
